from model.map import Map;


def get_one(name: str) -> Map :
    return {
            "data": [
                        [0, 0, 0], 
                        [0, 3, 0], 
                        [0, 0, 0]
                    ]
            }